\hypertarget{some_method-example}{}\doxysection{some\+Method}
Is the parameter optional?

Note this is distinct from \char`\"{}is\+Default\+Value\+Available\char`\"{} because you can have a default value, but the parameter not be optional. In the example, the \$foo parameter is\+Optional() == false, but is\+Default\+Value\+Available == true

(\$foo = \textquotesingle{}foo\textquotesingle{}, \$bar)


\begin{DoxyCodeInclude}{0}
\end{DoxyCodeInclude}
 